[project]
name = "avpr-portable-contract-tests"
version = "0.0.0"
description = "Python environment for AVPR's portable model and codec contract tests."
requires-python = ">=3.12"
dependencies = [
    "fable-library==5.11.0",
]

[tool.uv]
package = false
